import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            light: '#5cec51',
            main: '#00b819',
            dark: '#008600',
            contrastText: '#fff',
        },
    }
});

export default theme;